<Window x:Class="FolderIconManager.GUI.Dialogs.IconPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Icon" 
        Height="650" Width="900"
        MinHeight="500" MinWidth="700"
        Background="{StaticResource BackgroundDarkBrush}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Control -->
        <TabControl Grid.Row="0" Background="Transparent" BorderThickness="0">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="{StaticResource BackgroundMediumBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                    <Setter Property="Padding" Value="16,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" 
                                        Background="{TemplateBinding Background}" 
                                        CornerRadius="6,6,0,0" 
                                        Padding="{TemplateBinding Padding}"
                                        Margin="0,0,2,0">
                                    <ContentPresenter ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBlueBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>

            <!-- System Icons Tab -->
            <TabItem Header="🖥️ System Icons">
                <Border Background="{StaticResource BackgroundMediumBrush}" CornerRadius="0,6,6,6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Source selector and search -->
                        <Border Grid.Row="0" Background="{StaticResource BackgroundDarkBrush}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Source:" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                                
                                <ComboBox Grid.Column="1" x:Name="SourceComboBox"
                                          SelectionChanged="SourceComboBox_SelectionChanged"
                                          Background="{StaticResource BackgroundLightBrush}"
                                          Foreground="{StaticResource TextPrimaryBrush}"/>

                                <TextBox Grid.Column="2" x:Name="SearchBox"
                                         Style="{StaticResource ModernTextBox}"
                                         Margin="16,0"
                                         TextChanged="SearchBox_TextChanged"
                                         ToolTip="Search by icon index"/>

                                <StackPanel Grid.Column="3" Orientation="Horizontal">
                                    <TextBlock Text="Size:" 
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Slider x:Name="IconSizeSlider" 
                                            Width="100" Minimum="32" Maximum="64" Value="48"
                                            ValueChanged="IconSizeSlider_ValueChanged"
                                            VerticalAlignment="Center"/>
                                    <TextBlock x:Name="IconSizeText" Text="48" 
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               VerticalAlignment="Center" Margin="8,0,0,0" Width="25"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Icons Grid -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl x:Name="IconsGrid">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border x:Name="IconBorder" 
                                                Width="{Binding IconSize}" Height="{Binding IconSize}"
                                                Background="Transparent"
                                                BorderBrush="Transparent" BorderThickness="2"
                                                CornerRadius="4" Margin="4"
                                                Cursor="Hand"
                                                MouseLeftButtonDown="IconItem_MouseLeftButtonDown"
                                                MouseLeftButtonUp="IconItem_MouseLeftButtonUp">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                        </Trigger>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                                            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Image Source="{Binding Image}" 
                                                       RenderOptions.BitmapScalingMode="HighQuality"
                                                       Margin="6"/>
                                                <TextBlock Text="{Binding Index}" 
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           FontSize="9"
                                                           HorizontalAlignment="Right" 
                                                           VerticalAlignment="Bottom"
                                                           Margin="0,0,4,2"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Browse File Tab -->
            <TabItem Header="📂 Browse File">
                <Border Background="{StaticResource BackgroundMediumBrush}" CornerRadius="0,6,6,6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- File selector -->
                        <Border Grid.Row="0" Background="{StaticResource BackgroundDarkBrush}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" x:Name="BrowsePathBox"
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         Margin="0,0,8,0"/>
                                
                                <Button Grid.Column="1" Content="Browse..."
                                        Style="{StaticResource ModernButton}"
                                        Click="BrowseButton_Click"/>
                            </Grid>
                        </Border>

                        <!-- Icons from file -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="BrowseIconsGrid">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="{Binding IconSize}" Height="{Binding IconSize}"
                                                Background="Transparent"
                                                BorderBrush="Transparent" BorderThickness="2"
                                                CornerRadius="4" Margin="4"
                                                Cursor="Hand"
                                                MouseLeftButtonDown="IconItem_MouseLeftButtonDown"
                                                MouseLeftButtonUp="IconItem_MouseLeftButtonUp">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                        </Trigger>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                                            <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Image Source="{Binding Image}" 
                                                       RenderOptions.BitmapScalingMode="HighQuality"
                                                       Margin="6"/>
                                                <TextBlock Text="{Binding Index}" 
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           FontSize="9"
                                                           HorizontalAlignment="Right" 
                                                           VerticalAlignment="Bottom"
                                                           Margin="0,0,4,2"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </TabItem>

            <!-- Recent Tab -->
            <TabItem Header="🕐 Recent">
                <Border Background="{StaticResource BackgroundMediumBrush}" CornerRadius="0,6,6,6" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="RecentIconsGrid">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="64" Height="64"
                                            Background="Transparent"
                                            BorderBrush="Transparent" BorderThickness="2"
                                            CornerRadius="4" Margin="4"
                                            Cursor="Hand"
                                            MouseLeftButtonDown="IconItem_MouseLeftButtonDown"
                                            MouseLeftButtonUp="IconItem_MouseLeftButtonUp"
                                            ToolTip="{Binding SourcePath}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                    </Trigger>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                                                        <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Image Source="{Binding Image}" 
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               Margin="6"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Preview and Options -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundMediumBrush}" 
                CornerRadius="6" Padding="12" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Preview -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="64" Height="64" 
                            Background="{StaticResource BackgroundDarkBrush}"
                            CornerRadius="4" Margin="0,0,12,0">
                        <Image x:Name="PreviewImage" 
                               RenderOptions.BitmapScalingMode="HighQuality"
                               Margin="8"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="PreviewSourceText" 
                                   Text="No icon selected"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontWeight="SemiBold"/>
                        <TextBlock x:Name="PreviewIndexText"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"/>
                    </StackPanel>
                </StackPanel>

                <!-- Storage Mode -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Storage:" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <RadioButton x:Name="LocalRadio" Content="📥 Local (Extract)"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 IsChecked="True" Margin="0,0,16,0"
                                 ToolTip="Extract icon to folder.ico - portable, won't break"/>
                    <RadioButton x:Name="ExternalRadio" Content="🔗 External (Reference)"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 ToolTip="Keep reference to source file - may break if source moves"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" 
                    Style="{StaticResource ModernButton}"
                    Width="100" Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="ApplyButton" Content="Apply Icon" 
                    Style="{StaticResource PrimaryButton}"
                    Width="120"
                    IsEnabled="False"
                    Click="ApplyButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

